
<script>
// ES6 模块
/*
设计思想是在编译时就能确定模块的依赖关系，以及输入和输出的变量
分export 和 import

自动开户严格模式，不管你有没有在模块头部加上use strict
可以导入和导出各种类型的变量，如函数，对象，字符中，数字，布尔值，类等.
每个模块都有自己的上下文，每一个模块内声明的变量者是局部变量，不会污染全局作用域。
每一个模块只加载一次(单例)，若再去加载同目录下同文件，直接从内存中读取

# export 与 import
* 导出的函数声明与类声明必须要有名称(export default命令另外考虑)
* 不仅能导出声明还能导出引用(例如函数)
* export命令可以出现在模块的任何位置，但必须处理于模块顶层
* import命令提升到整个模块的头部，首先执行






*/



</script>
